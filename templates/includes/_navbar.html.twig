<nav class="custom-navbar border-b-accentcolor w-full border-b-2 h-16 font-titlefont z-50 relative">
  <div class="flex items-center justify-between p-4 relative">
    <a href="#" class="flex items-center mr-6">
      <span class="self-start text-2xl pl-4 font-semibold whitespace-nowrap text-white">SMARTBOOKING</span>
    </a>
    
    <!-- Texte centré -->
    {% if app.user %}
      <div class="absolute left-1/2 transform -translate-x-1/2 text-white">
        Vous êtes connecté en tant que {{ app.user.userIdentifier }}
      </div>
    {% endif %}
    
    <!-- Boutons à droite -->
    <ul class="hidden md:flex items-center space-x-4">
      <li class="group relative">
        <a href="#" class="inline-block px-4 py-2 text-white hover:text-accentcolor">Contact</a>
      </li>
      <li class="group relative">
        {% if app.user %}
          <a href="#" class="inline-block px-4 py-2 text-white hover:text-accentcolor">Mes réservations</a>
      </li>
      <li class="group relative">
        
        <div class="inline-block px-4 py-2 text-white hover:text-gray-200">
            <span>
              <a href="{{ path('app_logout') }}" class="text-white hover:text-accentcolor">Déconnexion</a>
            </span>
          </div>
      </li>
    {% endif %}
    
    </ul>
  </div>
</nav>
